import { styled } from "stitches.config";

export const Box = styled("div", {});

export const Container = styled("div", {
  display: "flex",
  height: "100%",
  width: "100%",
  overflowX: "hidden",
});
